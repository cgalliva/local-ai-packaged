# Credentials backup folder

Place exported n8n credentials here (optional).

**⚠️ WARNING**: Credential files may contain sensitive information!

Consider adding this to `.gitignore`:
```
n8n/backup/credentials/*.json
```

Only commit credential files if they:
- Use placeholder values
- Are templates for setup
- Don't contain real API keys or passwords

